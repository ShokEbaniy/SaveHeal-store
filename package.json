{
  "name": "merncrashcourse-backend",
  "version": "1.0.0",
  "main": "Backend/server.js",
  "type": "module",
  "scripts": {
    "dev": "cross-env NODE_ENV=development nodemon Backend/server.js",
    "build": "npm install && npm install --prefix Frontend/merncrashcourse && npm run build --prefix Frontend/merncrashcourse",
    "start": "cross-env NODE_ENV=production node Backend/server.js"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
